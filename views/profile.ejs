<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full  min-h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end gap=4">
            <a class="bg-blue-300 px-3 py-2 rounded-md inline-block mb-5 flex gap-4" href="">Home</a>
            <a class="bg-blue-300 px-3 py-2 rounded-md inline-block mb-5 gap-10" href="">About</a>
            <a class="bg-blue-300 px-3 py-2 rounded-md inline-block mb-5 gap-10" href="">Contact</a>
            <a class="bg-red-500 rounded-md px-3 py-2 inline-block mb-5" href="/logout">logout</a>
        </div>
        <div class="flex items-start gap-2">
            <div class="w-14 h-14  rounded-md overflow-hidden ">
                <img  class="w-full h-full object-cover" src="/images/upload<%= user.profilepic %>" alt="">
            </div>
    <div class="w-full mb-9 bold text-3xl">Hello <%=user.name %></div>
        </div>

    <h3 class="mb-5">You Can Create a New Post</h3>
    <form action="/post" method="post">
        <textarea  placeholder="What's On Your Mind?" class="block p-3 outline-none w-1/3resize-none bg-transparent border-2 rounded-md border-zinc-700"  name="content"></textarea>
        <input class=" mt-2 w-40 px-3 py-2 bg-blue-500 block rounded-md" type="SUBMIT" value="Create Post">
    </form>
    <div class="post mt-10">
        <h3 class="text-2xl">Your Posts</h3>
        <div class="postcontainer mt-5">
           <% user.post.forEach(function(post){ %>
         <div class="post w-1/3 p-4 mb-2 bg-zinc-700 roundrd-md border-2 border-zinc-500">
            <h4 class="text-blue-500 mb-2">@<%= user.username %></h4>
         <p class="text-sm"><%= post.content %></p>
         <div class="btns flex gap-2 mt-5">
            <a class="text-blue-500" href="">Like</a>
            <a class="text-blue-500" href="">Edit</a>
         </div>
         </div>
           <% }) %>
        </div>
       
    </div>
    </div>
</body>
</html>